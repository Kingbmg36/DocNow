# Dev/test-only dependencies — not installed in the production Docker image.
# Run `pip install -r requirements-dev.txt` before `pytest tests/ -v`.
-r requirements.txt

requests==2.34.1
pytest>=8.2,<9
pytest-asyncio==0.24.0
